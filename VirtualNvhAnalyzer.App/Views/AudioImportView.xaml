<UserControl x:Class="VirtualNvhAnalyzer.App.Views.AudioImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VirtualNvhAnalyzer.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="400">

    <Grid>
        <StackPanel Orientation="Vertical"  HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Audio Import" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
            <Button Content="Import Audio File" Width="150"  Margin="0,0,0,10"
                    Command="{Binding ImportAudioCommand}" />
            <TextBlock Text="{Binding SelectedFileName}" FontStyle="Italic" Foreground="Gray"/>

        </StackPanel>
    </Grid>
</UserControl>
